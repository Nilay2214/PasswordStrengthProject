# PasswordStrengthProject
PasswordStrengthProject is a Java-based application that evaluates user password strength based on length, character types, and complexity rules. It classifies passwords as weak, medium, or strong. The project uses Core Java for validation logic and MySQL with
